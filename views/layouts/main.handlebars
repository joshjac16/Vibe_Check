<html lang='en'>

  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Vibe Check</title>
    <link rel='stylesheet' type='text/css' href='/css/jass.css' />
    <link rel='stylesheet' type='text/css' href='/css/style.css' />
    <link rel='stylesheet' type='text/css' href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css' />
  </head>

  <body>
    <header>
      <img src='images/FullLogo.jpg' alt="partial square with a letter V in the center and the words 'Vibe Check' written below" width="300"></img>
    </header>
    <nav>
      <a href='/'>Home</a>
      {{#if loggedIn}}
        <button id='logout' class='btn-no-style'>Logout</button>
      {{else}}
        <a href='/login'>Login</a> or <a href='/signup'>Signup</a>
      {{/if}}
    </nav>
    <main>
      <div class=''>
        {{{body}}}
      </div>
    </main>
    {{#if loggedIn}}
      <script src='/js/logout.js'></script>
    {{/if}}
    <script src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js'></script>
  </body>

</html>